# Core scientific / FFT stack (high-performance DSP)
numpy>=1.26.0
scipy>=1.12.0
pyfftw>=0.13.0

# Audio I/O — lowest latency real-time + file support
sounddevice>=0.5.5
soundfile>=0.13.0
pydub>=0.25.0

# GPU shaders, particles, orbs, trails — the performance core
taichi>=1.7.0

# GPU-accelerated immediate-mode UI (panels, scrubber, HUD, queue)
dearpygui>=2.0.0

# Fast JSON for presets / share links
orjson>=3.10.0

# For video export or advanced image handling
imageio>=2.34.0
moviepy>=1.0.0   # requires ffmpeg installed separately
